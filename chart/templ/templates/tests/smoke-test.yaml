apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-smoke-test"
  annotations:
    "helm.sh/hook": test-success
    # (mpdev will replace this with marketplace.cloud.google.com/verification:test)
spec:
  restartPolicy: Never
  containers:
    - name: smoke-test
      image: busybox
      command: ["sh","-c","echo Smoke test passed"]